# Actor exports (in use)

Live Foundry VTT actor exports for the campaign. **Import the plain
`fvtt-Actor-*.json` files here** — they have been repaired by
`tools/sanitize-foundry-actor.py` (deterministic, no AI).

| File | Use |
|---|---|
| `fvtt-Actor-*.json` | Repaired, import-ready. Use these. |
| `fvtt-Actor-*-NO-SPECIES.json` | Same, with the species removed — import these when you intend to add a species through Plutonium. |
| `original-fvtt-Actor-*.json` | Untouched exports, kept as the regression corpus. Do not import. |
| `fvtt-Actor-lady-aurelian-corvinarus-PC.json` | Aurelian as a playable character — see below. |
| `fvtt-Actor-wario-motorbike.json` | Wario's motorbike as an NPC vehicle — see below. |

Every character now ships both variants:

| Character | Normal | Species-free |
|---|---|---|
| Archie Miser | 102 items, Human | 101 items |
| Eager | 57 items, Grung | 52 items |
| Feyward Dan | 62 items, Grung | 57 items |
| Hjumpik Deldkur | 102 items, Mountain Dwarf | 99 items |
| Markop Judi | 118 items, Centaur | 104 items |
| Remi | 132 items, Human | 127 items |
| Toad Lee | 55 items, Bullywug | 50 items |
| Waluigi | 52 items, Darakhul (Human/Half-Elf Heritage) | 39 items |
| Wario | 33 items, Human | 32 items |
| Lady Aurelian (PC) | 53 items, Fey (Corvinarus Bloodline) | 52 items |

Azure, Salam and the Aurelian NPC statblock are NPCs, which dnd5e does not
restrict to one species, so they have no variant. Wario's motorbike is
likewise an NPC — a vehicle, not a character — so it has no species variant
and no `-NO-SPECIES` copy.

## Intake: Players.json → originals → import-ready

The party's live export is `Players.json` in the repo root: a single file
holding all eleven actors. Foundry imports one actor per file, so the intake
pipeline splits it and feeds the existing rebuild:

```bash
python3 tools/fix-players-intake.py    # one-time fixes (idempotent): Dan's
                                       # background pointer, Wario NPC→PC
python3 tools/split-players.py         # Players.json → original-fvtt-*.json
python3 tools/rebuild-actors.py        # originals → import-ready + NO-SPECIES
python3 tools/rebuild-actors.py --check   # verify, write nothing
```

Per-actor decisions (Dan and Eager keeping the second species) live in
`OVERRIDES` in `rebuild-actors.py`, so nothing here is a hand-edited
artefact. The `--check` mode runs in `check-all.py` as **actor exports**,
and `tools/tests/test-players-split.py` guards the split itself (every
entry accounted for, originals JSON-equal to the live entries).

**Supersede policy:** the Players.json versions replace the older
single-actor originals for Dan, Hjumpik, Toad Lee and Waluigi (the old
files are retired; git history keeps them). Aurelian and Azure have no
live export, so their files stand. Wario's motorbike is **not** split —
it stays generated by `tools/build-wario-motorbike.py`, and the split
test fails on drift between the live entry and the generated file.

**Bridge:** `python3 tools/split-players.py --watch` polls Players.json
and re-splits + rebuilds whenever it changes, so the import-ready files
stay fresh while the export is being edited. Honest limitation: this
refreshes the files on disk — nothing can push data into a live Foundry
world, so the DM still imports the `fvtt-Actor-*.json` files by hand.

Regenerate any single file:

```bash
cd Reputation-Matrix2/actors
python3 ../../tools/sanitize-foundry-actor.py original-fvtt-Actor-NAME.json \
  --write fvtt-Actor-NAME.json
```

## What was repaired

| Actor | Repairs |
|---|---|
| Archie Miser | 2 dangling advancement grants, 25 orphan container refs, portrait + token → placeholders (see below) |
| Azure (Feywild Theater State) | 2 unresolvable icons → typed placeholders |
| Eager | duplicate species + background (kept **Grung** + **Slave**), 1 orphan container ref |
| Feyward Dan | duplicate species (kept **Grung**, see below) |
| Hjumpik Deldkur | already clean |
| Lady Aurelian Corvinarus | already clean, copied unchanged |
| Markop Judi | 20 dangling advancement grants, 7 orphan container refs, portrait + token + 2 icons → placeholders |
| Remi | 20 dangling advancement grants, 6 orphan container refs |
| Salam | portrait → placeholder |
| Toad Lee | already clean |
| Waluigi | already clean |
| Wario | 3 orphan container refs, portrait + token → placeholders |

Every file also drops stray world-user ownership (cosmetic). Item counts
preserved, `system` stat blocks byte-identical, second pass clean.

Portraits that exist in the live world but not in this repo's asset
manifest (`player/newarchie.webp`, `player/markop.png`, `npc/salam.png`,
`npc/wario.png`) become typed `icons/svg/*` placeholders on sanitize.
Re-add the image files and re-export rather than patching the JSON.

## If the species error persists after importing

Two things cause this, and the first one is not a bug in the file:

**1. You are importing an old copy.** The `tools/item sheet examples/sanitized/`
folder no longer exists — these files moved here. Pull the branch before
importing, and check the file you pick is from `Reputation-Matrix2/actors/`.

**2. A phantom species pointer.** dnd5e stores `system.details.race` next to
the race item. If that pointer names an item id that is not in `items[]`, the
sheet believes a species exists that you cannot see or delete on the sheet,
and `Race._preCreate` refuses the new one. **The original exports carried
dangling or mislabelled pointers of this kind**, so this was almost certainly the real blocker.
The sanitizer now relinks or clears them (`dangling-detail-pointer`,
`detail-pointer-relinked`), and `--clear-species` clears the pointer along with
the item. Dan's background pointer (free text instead of the Dimensional
Escapee item id) is repaired one step earlier, at intake by
`tools/fix-players-intake.py`, so the sanitizer never sees the broken state.

Free-text values such as `race: "Archfey-Touched"` are legal on **NPCs** and
are left alone. On a **character** the same field is a document pointer, so a
label there (Toad Lee carried `race: "Toad"` while owning a real species item)
is the same phantom-species state and gets relinked to the actual item.

If it still fails, delete the species on the sheet by hand, save, and re-run
the Plutonium import.

## "Only a single Species can be added to a Player Character"

**This is not a defect in the file and no amount of sanitizing fixes it.**

Hjumpik already has a species (`Mountain Dwarf`). dnd5e's `Race._preCreate`
refuses to create a second one, Plutonium's task still expects every requested
document back, and the shortfall surfaces as
`Number of returned items did not match number of input items!` — a misleading
message for a plain refusal.

To import a new species onto any character, the existing one has to be gone
**first**. Either delete it on the sheet and re-run the Plutonium import, or
import that character's `-NO-SPECIES.json`, which has the species, its racial
features and the `details.race` pointer already removed:

```bash
python3 ../../tools/sanitize-foundry-actor.py \
  original-fvtt-Actor-hjumpik-deldkur-Qir5aDX8bkL5lt1c.json \
  --clear-species \
  --write fvtt-Actor-hjumpik-deldkur-Qir5aDX8bkL5lt1c-NO-SPECIES.json
```

That file drops `Mountain Dwarf` plus one copy each of the doubled
`Dwarven Resilience` and `Stonecunning` feats (102 → 99 items). It leaves the
surviving `feat`-typed copies and `Darkvision` in place: a plain feat can just
as easily be a class ability, so deleting by name would risk real content. The
PHB'24 Dwarf import will re-add its own copies — delete the leftovers by hand
if they stack.

## Feyward Dan — a judgement call

Dan carried two `race` items: `Toad — Feyward Variant` (0 advancements,
136-char description) and `Grung` (3 advancements, climb speed, 766 chars).
Default keep-first would have kept the near-empty one, so this file is built
with `--keep-species last`. **If Toad is the intended species, regenerate
without that flag and re-add the traits by hand.**

## Eager — the same call, twice

Eager doubles up on both axes: `Toad — Eager Variant`/`Grung` and
`Disaster Inc. Catastrophe Scout`/`Slave`. His own `details` pointers select
the later of each pair, so `--keep-species last` honours the filed choice and
keeps **Grung** + **Slave** with no pointer relink needed. **If the Disaster
Inc. background is the intended one, flip the flag — and tell the DM the
details pointers disagree, because the live sheet shows Slave.**

## Known remaining 404s (not fixable here)

Markop's sheet references two assets that exist in no install:
`1709761629520545.jpg` (previously on Hjumpik's retired export) and an
AI-generated item icon
(`ofmfwui4eg0jvc28-generated_image-removebg-preview.webp`) — now typed
`icons/svg/*` placeholders. Re-export rather than patching the JSON.
(Hjumpik's other old 404s — `321.webp`, the plutonium icon, the spell-points
mana icon — are gone with the retired export; the spell-points icon returns
if that module is reinstalled.)

## Lady Aurelian as a player character

`fvtt-Actor-lady-aurelian-corvinarus-PC.json` is generated by
`tools/build-aurelian-pc.py` (deterministic, no AI). The NPC statblock is kept
beside it and is still the version to use when she is run as an antagonist.

```bash
python3 ../../tools/build-aurelian-pc.py            # write the PC export
python3 ../../tools/build-aurelian-pc.py --check    # verify, write nothing
python3 ../../tools/build-aurelian-pc.py --keep-boss-abilities
```

**Level 9 Bard, College of Glamour.** Every choice comes from her own data
rather than invention:

| Decision | Evidence |
|---|---|
| Level 9 | Her `attributes.spell.level` is already 9 and her slots are exactly 4/3/3/3/1 — the level 9 full-caster row. |
| Bard | **25 of her 28 spells are on the Bard list** — next best is Wizard at 19. `attributes.spellcasting` is already `cha`; CHA 20 is her best stat. See `tools/analyze-aurelian-class.py`. |
| College of Glamour | Mantle of Inspiration and Enthralling Performance are mechanically the "delicate hostess" mask her biography describes. |
| HP 66, not 97 | 97 was `15d8+30`, a CR-8 monster budget. A level 9 d8 Bard gets 8 + (8 × 5) + (9 × CON 2) = 66. |
| AC via the gown | Player characters cannot use `calc: "natural"`, so Gown of Autumn's Last Light carries the AC. |

**Dropped as monster-only:** Legendary Resistance (2/Day), Multiattack, Innate
Spellcasting, and the NPC `Spellcasting` feat (replaced by the real class
feature). Pass `--keep-boss-abilities` to retain them.

**Kept:** all 28 spells, her three signature items, all three loot pieces, and
the flavour features Delicate Hostess, Draconic Heritage, Dimensional Anchor
and Wither and Bloom. `Druidcraft`, `Thaumaturgy` and `Wall of Fire` are the
only three spells off the Bard list, so they are tagged **Magical Secrets**
rather than removed. (`Plant Growth` and `Speak with Plants` *are* Bard
spells — an earlier build tagged them by mistake.) Her fey species (flight 50 ft, hover, darkvision 120 ft,
charm/fright immunity) is preserved as a proper `race` item.

She keeps 5 hit dice worth of nothing else: 53 items total, and the file passes
the sanitizer clean.

### Why Bard, in numbers

`tools/analyze-aurelian-class.py` scores every full-caster list against the
28 spells she already had. It is pure counting — the only judgement is the
hand-entered spell/class table, and spells whose 2014 and 2024 lists differ
are reported separately so an edition disagreement cannot change the answer
silently.

| Class | Coverage (2024 PHB) |
|---|---|
| **Bard** | **25 / 28 — 89%** |
| Wizard | 19 / 28 — 68% |
| Sorcerer | 18 / 28 — 64% |
| Warlock | 12 / 28 — 43% |
| Druid | 12 / 28 — 43% |
| Cleric | 6 / 28 — 21% |

A six-spell gap to the runner-up is not close. Her school profile is
**57% enchantment and illusion** (10 enchantment, 6 illusion), which is the
Bard's core identity, and the three misses are trivially covered by Magical
Secrets.

Warlock deserves a mention because Archfey patron matches her fiction well,
but it covers only 12 of her spells and pact magic gives 2 slots where her
sheet already specifies 4/3/3/3/1. It would mean rewriting her spellcasting
from scratch rather than expressing what is already there.

## Waluigi — generated file retired

The old `fvtt-Actor-waluigi-PC.json` (built by `tools/build-waluigi-pc.py`
from the wiki, back when no live export existed) is **retired**. The live
Waluigi export in Players.json — 52 items, actually played — supersedes it,
so the import-ready file is now
`fvtt-Actor-waluigi-BmWNDwbxPQHU3Bbn.json` like everyone else. The builder
script stays in `tools/` for reference, but `rebuild-actors.py` no longer
runs it. (The retired build was a level 5 Evocation Wizard at 37 HP; the
live sheet is the Darakhul-heritaged original — trust the game, not the
reconstruction.)

## Wario as a player character

Wario shipped in Players.json as an NPC statblock (CR 7, `actorLink: false`).
`tools/fix-players-intake.py` converts him to a full `character` before the
split — the conversion is in code, re-runnable and idempotent, not a
hand-edit.

**Level 7 Barbarian (Berserker), Criminal, Human.** Every choice comes from
his own sheet:

| Decision | Evidence |
|---|---|
| Level 7 | CR 7 maps to level 7; XP set to 23,000, the level 7 threshold (matches Remi). |
| Barbarian | STR 20 is his best stat by 4+ and his whole kit is unarmed strikes, a shoulder charge and thrown bombs — rage with extra steps. |
| Berserker | Multiattack + Frenzy-shaped aggression; the biography files him as a brawler, not a tactician. |
| Criminal | Thieves' tools proficiency plus a bomb workshop and a rap sheet. |
| Human | His own `details.race` free text read `Human (Mushroom Kingdom)` — now a real item. |

**Kept:** all 29 original items (including Multiattack and the Wario Bike
summon), HP 136, flat AC 16, the full biography, token art and ownership.
**Minted:** Human / Barbarian / Path of the Berserker / Criminal items with
**emptied advancement shells** — the DM completes hit dice, ASIs and
level-up choices inside Foundry after import. `actorLink` flips to `true`;
NPC-only keys (`cr`, `type`, `habitat`, `treasure`, `source`) are stripped
and PC blocks (`bastion`, `favorites`, full `details`) added.

## Wario's motorbike as an NPC

`fvtt-Actor-wario-motorbike.json`, generated by
`tools/build-wario-motorbike.py`. This is the physical motorbike from the
Scorncrow Skirmish record — the one Wario rode into the grove and stalled in
the leaves and branches.

```bash
python3 ../../tools/build-wario-motorbike.py            # write the NPC export
python3 ../../tools/build-wario-motorbike.py --check    # verify, write nothing
```

**Type `npc`, 8 items.** It is a vehicle, not a character, so there is no
species and no `-NO-SPECIES` variant.

| Decision | Evidence |
|---|---|
| AC 16, HP 75, speed 120 ft | The "Wario's Motorcycle (Customized)" item record on Wario's own sheet (`tools/item sheet examples/wario.json`) — the customized getaway bike with the rear compartment and saddlebags the Scorncrow record shows. |
| CR blank | Vehicles have no challenge rating. |
| Abilities neutral 10s | A vehicle has no ability scores of its own; dnd5e requires the six fields anyway. The Ramming Speed attack rolls flat and says to use the driver's Strength + proficiency. |
| AC `calc: "flat"` | The 16 comes from armor plating / reinforced frame, matching Wario's own actor (`calc: "flat"`, not `"natural"`). |
| No difficult-terrain immunity | The chopper record claims it; the customized record does not, and the Scorncrow record proves the opposite — the motorbike stalled in the leaves and branches. |
| Token art `npc/ai/wario-bike.webp` | Already present in the campaign's Foundry data manifest, referenced by the same Foundry-relative convention as the other exports. |

**Other records of the same machine are not merged in.** Wario's sheet also
carries a weaponized "Custom Chopper" configuration (AC 16 / 75 HP / 70 ft,
flamethrower, spiked wheel guards, oil slick) and a summoned-mount block from
Wario Bike Summon (AC 14 / 50 HP / 60 ft). Both are documented inside the
actor and in `tools/build-wario-motorbike.py` rather than combined, because
no single record combines them.
